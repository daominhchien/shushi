{
  "name": "json_server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"json-server --watch db.json\" \"node server.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "concurrently": "^8.0.1"
  }
}